# Safety CLI policy. bankstatementparser-writer-xlsx has a small dependency tree
# (openpyxl, pandas, bankstatementparser + its transitive deps); pin known false
# positives here with an expiry so they don't go stale.
security:
  ignore-vulnerabilities: {}
